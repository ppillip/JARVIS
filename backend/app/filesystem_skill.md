# filesystem_skill.py

Filesystem MCP 결과를 사용자 의도에 맞게 해석하는 skill 레이어입니다.

- `list_directory`, `directory_tree`, `read_text_file` 결과 파싱
- 폴더 목록, 파일 개수, 최신 파일 같은 의도 처리
- 실행 결과를 `findings`, `evidence`, `result_items` 형태로 정리
